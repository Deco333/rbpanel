<Window x:Class="KidsWinUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KidsWin Executor" 
        Height="700" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E20"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="#2D2D30" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="KidsWin Executor" FontSize="24" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <TextBlock x:Name="StatusText" Text="Status: Not connected" FontSize="14" Foreground="#888888" Margin="0,5,0,0"/>
                    <TextBlock x:Name="ProcessInfo" Text="PID: --" FontSize="12" Foreground="#666666" Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="AttachButton" Content="📎 Attach" Click="AttachButton_Click" Width="120" Margin="0,0,10,0"/>
                    <Button x:Name="DetachButton" Content="❌ Detach" Click="DetachButton_Click" Width="120" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Script Editor -->
        <Border Grid.Row="1" Background="#2D2D30" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Lua Script Editor" FontSize="16" FontWeight="SemiBold" Foreground="#CCCCCC" VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="📂 Load" Click="LoadScript_Click" Width="80" Margin="0,0,5,0"/>
                        <Button Content="💾 Save" Click="SaveScript_Click" Width="80" Margin="0,0,5,0"/>
                        <Button Content="🗑 Clear" Click="ClearScript_Click" Width="80"/>
                    </StackPanel>
                </Grid>

                <TextBox x:Name="ScriptEditor" 
                         Grid.Row="1" 
                         AcceptsReturn="True" 
                         AcceptsTab="True" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"
                         MinHeight="200"/>
            </Grid>
        </Border>

        <!-- Console Output -->
        <Border Grid.Row="2" Background="#2D2D30" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Console Output" FontSize="16" FontWeight="SemiBold" Foreground="#CCCCCC" Margin="0,0,0,10"/>
                
                <TextBox x:Name="ConsoleOutput" 
                         Grid.Row="1" 
                         IsReadOnly="True" 
                         VerticalScrollBarVisibility="Auto" 
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Background="#1A1A1D"
                         Foreground="#00FF00"
                         FontFamily="Consolas"
                         FontSize="12"/>
            </Grid>
        </Border>

        <!-- Execute Button -->
        <Border Grid.Row="3" Background="#2D2D30" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           x:Name="ErrorText" 
                           Text="" 
                           Foreground="#FF5555" 
                           FontSize="13" 
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"/>

                <Button x:Name="ExecuteButton" 
                        Grid.Column="1" 
                        Content="▶ Execute" 
                        Click="ExecuteButton_Click" 
                        Width="150" 
                        FontSize="16"
                        IsEnabled="False"
                        Background="#6A9955"/>
            </Grid>
        </Border>
    </Grid>
</Window>
